# Mission: Reading Stats API

## Goal
`GET /api/v1/stats` 엔드포인트를 구현한다.
사용자별 읽기 통계(kept/deleted/skipped 수)를 반환해야 한다.

## Success Criteria
1. `src/api/routers/stats.py` 파일이 존재한다
2. `/api/v1/stats` 엔드포인트가 200을 반환한다
3. 응답에 `kept`, `deleted`, `skipped` 필드가 포함된다
4. 관련 테스트가 존재하고 통과한다

## Max Iterations
3
